name: Nova Bank Security Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  snyk-security-validation:
    runs-on: ubuntu-latest

    steps:
      - name: Clone Repository into GitHub Actions Runner
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Repository Dependencies
        run: npm install

      - name: Install and Authenticate Snyk
        run: |
            npm install snyk -g
            snyk auth ${{ secrets.SNYK_TOKEN }}

      - name: Run Snyk Scan to Catch Vulnerabilities
        run: snyk test || true

      - name: Send Monitoring Snapshot
        run: snyk monitor

        #Pipeline Test

